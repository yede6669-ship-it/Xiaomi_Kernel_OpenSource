name: Aghisna R20 Camellia Deep Fix

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source
      uses: actions/checkout@v4
      with:
        ref: camellia-r-oss
        fetch-depth: 1

    - name: Setup Environment
      run: |
          sudo apt-get update
          sudo apt-get install -y git make bc bison \
          openssl curl zip kmod cpio flex libelf-dev \
          libssl-dev libtfm-dev wget device-tree-compiler \
          ca-certificates python-is-python3 binutils \
          binutils-aarch64-linux-gnu binutils-arm-linux-gnueabi

    - name: Build Kernel
      run: |
          # 1. 强制确定当前位置
          cd $GITHUB_WORKSPACE
          echo "Checking directory: $(pwd)"
          
          # 2. 检查 Makefile，如果不在根目录则递归搜索
          if [ ! -f "Makefile" ]; then
            echo "Warning: Makefile not in root. Finding..."
            M_PATH=$(find . -name "Makefile" -printf '%h' -quit)
            if [ -n "$M_PATH" ]; then
                echo "Found Makefile in $M_PATH, moving everything to root..."
                cp -af $M_PATH/. .
            else
                echo "FATAL: Makefile absolutely missing!"
                ls -R
                exit 1
            fi
          fi

          # 3. 下载脚本并注入 MTK 补丁
          curl -LSs https://raw.githubusercontent.com/RooGhz720/RooGhz720/main/rx/miui-r20.sh > miui-r20.sh
          chmod +x miui-r20.sh
          
          # 适配 Camellia 参数
          sed -i 's/DEVICE="Redmi note 10 pro"/DEVICE="Redmi Note 10 5G"/g' miui-r20.sh
          sed -i 's/DEFCONFIG="sweet_defconfig"/DEFCONFIG="camellia_defconfig"/g' miui-r20.sh
          
          # 核心修复：针对 MTK 源码在云端的 Stack Protector 报错
          # 在执行 make 后强制禁用堆栈检查
          sed -i '/make "$DEFCONFIG" O=out/a \          sed -i "s/CONFIG_CC_STACKPROTECTOR_STRONG=y/CONFIG_CC_STACKPROTECTOR_NONE=y/g" out/.config\n          make O=out olddefconfig' miui-r20.sh

          # 阻止脚本清理
          sed -i 's/rm -rf out/#rm -rf out/g' miui-r20.sh
          sed -i 's/rm -rf zip/#rm -rf zip/g' miui-r20.sh

          # 4. 执行编译
          bash miui-r20.sh
      shell: bash
      env:
         CHATID: ${{ secrets.CHAT_ID }}
         API_BOT: ${{ secrets.BOT_API }}

    - name: Upload Artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: Camellia-Built-Kernel
        path: |
          zip/*.zip
          out/arch/arm64/boot/Image*
          out/arch/arm64/boot/dtbo.img
          error.log
