name: Stable Xiaomi Kernel Build (Camellia)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: '内核分支 (请确保分支内有 Makefile)'
        required: true
        default: 'camellia-r-oss'

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ARCH: arm64
      SUBARCH: arm64
      OUT_DIR: out
      # 强制宿主机工具使用 GCC 12 以兼容旧内核脚本
      CC: gcc-12
      CXX: g++-12

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        ref: ${{ github.event.inputs.branch }}
        submodules: true

    - name: Install Toolchain & Dependencies
      run: |
        sudo apt update
        # 安装 gcc-12 以及交叉编译器
        sudo apt install -y \
          bc bison flex libssl-dev libelf-dev \
          build-essential git curl wget \
          gcc-12 g++-12 \
          clang llvm lld gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi

    - name: Detect defconfig
      run: |
        DEFCONFIG_PATH=$(find arch/arm64/configs -name "*camellia*defconfig" | head -n 1)
        if [ -z "$DEFCONFIG_PATH" ]; then
          DEFCONFIG_PATH=$(find arch/arm64/configs -name "defconfig" | head -n 1)
        fi
        DEFCONFIG_NAME=$(basename "$DEFCONFIG_PATH")
        echo "DEFCONFIG=$DEFCONFIG_NAME" >> $GITHUB_ENV

    - name: Fix Source Bugs
      run: |
        # 移除 MTK VCU 重复定义
        FILE="drivers/media/platform/mtk-vcu/Makefile"
        if [ -f "$FILE" ]; then
          sed -i 's/mtk_vcu_mem.o//g' "$FILE" || true
        fi
        
        # 针对新版 GCC 的脚本兼容性修复 (消除一些会导致中断的警告)
        sed -i 's/-Werror//g' Makefile || true

    - name: Generate Config
      run: |
        mkdir -p $OUT_DIR
        # 使用 HOSTCC 指定宿主机编译器为 gcc-12
        make O=$OUT_DIR ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- HOSTCC=gcc-12 $DEFCONFIG

    - name: Build Kernel
      run: |
        # 联发科内核编译命令
        make -j$(nproc) O=$OUT_DIR \
          ARCH=arm64 \
          HOSTCC=gcc-12 \
          CC=clang \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
          LD=ld.lld \
          AR=llvm-ar \
          NM=llvm-nm \
          OBJCOPY=llvm-objcopy \
          OBJDUMP=llvm-objdump \
          STRIP=llvm-strip \
          AS=aarch64-linux-gnu-as \
          LLVM=1 \
          LLVM_IAS=1

    - name: Upload Kernel Artifact
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: Camellia-Kernel
        path: |
          ${{ env.OUT_DIR }}/arch/arm64/boot/Image
          ${{ env.OUT_DIR }}/arch/arm64/boot/Image.gz
          ${{ env.OUT_DIR }}/arch/arm64/boot/Image.gz-dtb
